import { ComingSoon } from "../components/ui/ComingSoon";

export function OverviewPage() {
  return (
    <ComingSoon
      title="Overview"
      note="Revenue position, recovery posture, outcome distribution, and recent activity — built in the next increment against GET /metrics/summary and GET /payments."
    />
  );
}
