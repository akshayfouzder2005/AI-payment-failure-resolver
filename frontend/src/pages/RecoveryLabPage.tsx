import { ComingSoon } from "../components/ui/ComingSoon";

export function RecoveryLabPage() {
  return (
    <ComingSoon
      title="Recovery Lab"
      note="Scenario builder + live execution trace against POST /simulate/failed-payment — built in a later increment."
    />
  );
}
