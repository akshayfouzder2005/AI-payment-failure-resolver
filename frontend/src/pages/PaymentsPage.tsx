import { ComingSoon } from "../components/ui/ComingSoon";

export function PaymentsPage() {
  return (
    <ComingSoon
      title="Payments"
      note="Filterable, paginated payment list — built against GET /payments in the next increment."
    />
  );
}
